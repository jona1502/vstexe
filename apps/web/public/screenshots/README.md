# Screenshots

Drop a capture of the desktop window here as `app.png` (1180x720 or larger, the window size the app opens at,
window only, no desktop background). `ProductPreview.astro` picks it up at
build time and replaces the built-in window representation with it.
